# ===========================================
# 프로덕션 환경 변수 (.env.production)
# 이 파일을 .env로 복사하고 값을 수정하세요
# ===========================================

# 도메인 설정
DOMAIN=your-domain.com
FRONTEND_URL=https://your-domain.com

# 데이터베이스
DB_USER=postgres
DB_PASSWORD=your-secure-password-here
DB_NAME=jangan_volunteer

# JWT 시크릿 키 (openssl rand -hex 32 로 생성)
SECRET_KEY=your-super-secret-key-generate-with-openssl

# Google OAuth
# https://console.cloud.google.com/ 에서 생성
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=https://your-domain.com/api/auth/social/google/callback

# Kakao OAuth
# https://developers.kakao.com/ 에서 생성
KAKAO_CLIENT_ID=your-kakao-rest-api-key
KAKAO_CLIENT_SECRET=your-kakao-client-secret
KAKAO_REDIRECT_URI=https://your-domain.com/api/auth/social/kakao/callback

# Firebase (푸시 알림) - 선택사항
# https://console.firebase.google.com/ 에서 생성
FIREBASE_CREDENTIALS_PATH=./firebase-credentials.json

# Frontend 빌드용
VITE_API_URL=/api

# 초기 관리자 계정
INITIAL_ADMIN_EMAIL=ruichela521@gmail.com
INITIAL_ADMIN_PASSWORD=DBtndls521!!
INITIAL_ADMIN_NAME=관리자
